-- Function to handle button clicks
local function onButtonClicked(button)
    print("Button clicked: " .. button.Text)
    -- Add code to open corresponding section here
end

-- Create the UI elements
local player = game.Players.LocalPlayer
local ui = Instance.new("ScreenGui")
ui.Parent = player.PlayerGui

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0.3, 0, 0.8, 0)
frame.Position = UDim2.new(0.7, 0, 0.1, 0) -- Adjusted position to top right
frame.BackgroundColor3 = Color3.new(0, 0, 0)
frame.BorderSizePixel = 0
frame.Parent = ui

-- Add title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0.1, 0)
title.Position = UDim2.new(0, 0, 0, 0)
title.BackgroundColor3 = Color3.new(1, 1, 1)
title.BorderSizePixel = 0
title.Text = "Rinn's Script (BSS)"
title.TextColor3 = Color3.new(0, 0, 0)
title.Font = Enum.Font.SourceSans
title.TextSize = 18
title.Parent = frame

-- Create buttons
local button1 = Instance.new("TextButton")
button1.Size = UDim2.new(1, 0, 0.1, 0)
button1.Position = UDim2.new(0, 0, 0.1, 0)
button1.BackgroundColor3 = Color3.new(1, 1, 1)
button1.BorderSizePixel = 0
button1.Text = "Section 1"
button1.TextColor3 = Color3.new(0, 0, 0)
button1.Parent = frame

local button2 = button1:Clone()
button2.Position = UDim2.new(0, 0, 0.2, 0)
button2.Text = "Section 2"
button2.Parent = frame

-- Connect buttons to click event
button1.MouseButton1Click:Connect(function()
    onButtonClicked(button1)
end)

button2.MouseButton1Click:Connect(function()
    onButtonClicked(button2)
end)
